#import "EventContext.graphql"

query GetEventContext(
  $applicationName: String!,
  $originatorId: UUID!,
  $originatorVersion: Int!
) {
  event(
    applicationName: $applicationName,
    originatorId: $originatorId,
    originatorVersion: $originatorVersion
  ) {
    ...EventContext
  }
}
